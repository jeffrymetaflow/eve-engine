# The API Key from your OpenAI Dashboard
OPENAI_API_KEY = "sk-..."

# Use "gpt-4o" for best reasoning or "gpt-4o-mini" for speed/lower cost
OPENAI_MODEL = "gpt-3.5-turbo"

# Optional: Set a default currency for the UI
DEFAULT_CURRENCY = "USD"
